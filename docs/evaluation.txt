What works:
-All menu options produce correct output for valid input.

-Missing or invalid input file is handled without crashing.

-Data exports correctly to output file in specified format.

-Vector class passes all unit tests including resizing, copying, and accessing elements.

-Date and Time classes store and return values correctly.

-Objects behave correctly when passed by value, reference, and const reference.

What does not work:
-Date class allows invalid days, months, and years without validation.

-Time class accepts values outside normal hour and minute ranges.

